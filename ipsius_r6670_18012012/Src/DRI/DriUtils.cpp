#include "stdafx.h"

#include "driutils.h"

#include "ISessionCmdContext.h"

// -----------------------------------------------

namespace DRI
{

    /*
    bool DriUtils::IsSpecialTypeParam( QString typeName )
    {
        if (QtTypename<ISessionCmdContext*>() == typeName) return true;
        if (QtTypename<ICmdOutput*>() == typeName) return true;

        return false;
    } */

}  // namespace DRI
