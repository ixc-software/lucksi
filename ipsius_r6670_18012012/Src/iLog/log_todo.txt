
* тесты
* адапция логирования в ISDN; инфораструктура должна иметь метод для создания сессий
  см. IIsdnLog.h
* ISDN -- убрать включение LogWrapperInterfaces.h



* LogStore.
  Попробовать все расширения сделать за счет класса-стратегии, 
  вопрос -- интерфейс взаимодействия с классом-хранилищем
  Мощные возможности выборок (полиморфно); сортировка по дате и тэгу сессии
  Должен мощно полимофно (?) расширяться в плане вывода (на экран, синхрониазация с файлом на диске)
  Переодическиое скидывание данных лога из памяти в файл (выгрузка)
  Добавить свойство для класса, строка Name (?)
  System tag detection -- через статическу функцию SystemTagDesc()
    

* Интерейсы конечного пользователя (на уровне сессии?). 
  Mixin с созданный в ней сессией для наследования
  Возможность включить/выключить лог, свойство.
  Сбор сессии в главный лог.
  Набор абстрактных интерфейсов, за которыми можно будет спрятать конкретные 
  реализации (на шаблонах) в .cpp файлах -- развязка для конкрентной имплементации 

     
* реализация для std::wstring и QString, взаимная конвертация этих типов строк
* Logger для QString имплементации с поддержкой std::stringstream
* Qt << support for std::string, std::wstring

     
* LogSession
  Примеры реальных стратегий для ILogSessionSync
  Подключить реальную проверку контекста потока -- через класс ThreadContext
  Допускать пустые имена (?)
     
* Logger
  Реализация для QTextStream, QString, std::stringstream etc.
               	          

- Timestamp должен в коструктор получать некий указатель на LogStore, например
  для возможности реализации простого счетчика или "относительного" времени
  (в тиках, относительно сохраненной полной даты)
- LogRecord atomic number -- быстрый способ отмечать порядок создания записей
  без timestamp в разных сессиях; проблема -- операция не выполняется унарно
- добавить возможность прямо в TRecord ресолвить session tag - ?
- Qt meta enum -> использование в качестве типа записи
- для std::list<LogRecord*> сделать класс, с вариантами managed/unmanaged - ?

                        

