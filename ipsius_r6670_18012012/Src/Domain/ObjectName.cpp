
#include "stdafx.h" 
#include "ObjectName.h"
#include "DomainClass.h"

namespace Domain
{


    // -----------------------------------------------------

    /*
    void ObjectName::SetParent(const ObjectName &name)
    {
        ESS_ASSERT(ObjectLevel() == 1);

        std::string prevName = m_name;

        m_name = name.m_name;
        m_name.append(1, Identificator::CNameSeparator); 
        m_name.append(prevName);
    } */

}  // namespace Domain

